import AccountsList from "@/features/accounts/components/AccountsList";

function AccountsPage() {
  // handles the screen state by wiring the form
  return <AccountsList />;
}

export default AccountsPage;
